_target_: src.models.factory.yolo
model_name: yolov8n
num_classes: 8

# Стартовые веса: локальная копия COCO-претрейна. Раньше значение жило только
# в дефолте фабрики (имя без пути) — не попадало в снапшот запуска и тянулось
# из сети в каталог каждого прогона. null — случайная инициализация.
weights: data/weights/yolov8n.pt
