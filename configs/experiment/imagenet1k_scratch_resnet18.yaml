# @package _global_
defaults:
  - override /data: imagenet1k
  - override /model/teacher: null
  - override /model/student: resnet18_imagenet1k
  - override /loss: ce
  - override /optimizer: adam
  - override /scheduler: cosine

name: imagenet1k_resnet18_scratch
device: cuda
deterministic: false

clearml:
  tags: [imagenet-1k, baseline, scratch, resnet18]

trainer:
  epochs: 20
  amp: true

optimizer:
  lr: 1.0e-4
