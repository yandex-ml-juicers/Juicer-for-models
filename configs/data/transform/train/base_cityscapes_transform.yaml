_target_: src.data.transforms.build_base_transform_for_cityscapes
mean: ${data.dataset.normalize.mean}
std: ${data.dataset.normalize.std}
train: True
image_size: ${data.dataset.image_size}

# 0.0 — выключено; включается в конкретном эксперименте, чтобы не менять
# аугментации уже проведённым прогонам на этом же трансформе.
horizontal_flip: 0.0
