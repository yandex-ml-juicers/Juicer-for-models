# Требуется сборка PyTorch с CUDA >= 12.8: torch 2.7 — первая версия
# с колёсами cu128. На GPU-машине ставить с правильным индексом
# (см. README, раздел "Установка"):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# ВАЖНО: саму опцию --index-url сюда писать нельзя — этот файл читает
# pyproject.toml (dynamic dependencies), setuptools ждёт только спецификаторы.
torch>=2.7
torchvision>=0.22
hydra-core>=1.3,<1.4
numpy>=1.24
tqdm>=4.65
timm>=1.0
detectors>=0.1.4
clearml>=1.16
